# Dogfood: Entry Window Radar

## As-of

2026-07-02 JST

## Target

entry-window-radar

## Tool Route

AI-assisted prompt route using `prompts/ai_assisted_entry_window_prompt.md`.

## Result Card

### Entry Label

NICHE WEDGE

### Market Line

There is a visible workflow need for lightweight, bounded entry-window diagnosis before a builder commits to a repo, idea, or small project. The repo now supports both terminal use and chat-first use, which makes the first interaction easier for users who already work inside Codex / GPT / Claude Code. Evidence for actual external demand is still thin.

### Competition Line

The broad space is crowded with startup idea scoring prompts, market research agents, README reviewers, strategy canvases, and generic AI copilots. Entry Window Radar is more narrowly framed as an As-of, non-predictive diagnosis with explicit Market / Competition / Operator Edge lines, but it still needs external comparison against generic alternatives.

### Operator Edge Line

Operator Edge appears meaningful because the repo carries Decision-OS / V13 governance practice, durable re-entry rules, local-only constraints, a working CLI, a worked example, and dogfood evidence. The AI-assisted route makes that operator-specific framing easier to use without requiring a first-time user to start in the terminal.

### Missing Evidence

- independent users trying the AI-assisted prompt on their own repo, README, idea, or small project
- first-time reader completion of the prompt route without maintainer explanation
- comparison against generic startup scoring prompts, README analyzers, and AI market-research workflows
- evidence that users understand the As-of boundary and do not read the card as prediction or investment advice
- external response quality before any SNS announcement

### Maximum Bottleneck

Independent user proof

### Next Action

Have one first-time user run the AI-assisted prompt on a real target and report whether the Entry Window Card was understandable without terminal help.

### Confidence

Medium

### Risk

The AI-assisted route may still be mistaken for a generic startup scoring prompt unless the As-of, non-predictive boundary remains prominent. There is also a risk that chat-only users skip evidence gathering and overread a thin-evidence card.

### Recheck Condition

Recheck after one first-time user completes the AI-assisted route on a real target and reports where the prompt, README, or result card was unclear.

## What Improved After Phase 7.1

- Users no longer need to begin with the terminal to understand the workflow.
- The prompt names the required input material before asking for a diagnosis.
- Chat-only mode and repo-assisted mode are separated.
- The output shape is visible before the CLI is run.
- The prompt repeats the As-of, non-predictive boundary and keeps Operator Edge first-class.

## What Remains Weak Before SNS Announcement

- No independent user has yet completed the AI-assisted route.
- The repo has not been compared against generic AI scoring or README-review alternatives.
- The README points to the AI-assisted route, but first-screen comprehension by an outside reader is untested.
- There is no evidence yet that external users will preserve the As-of boundary when sharing results.
- The announcement angle should wait until one real outside-use proof point exists.

## Why This Is Not External Posting

This note is an internal dogfood record committed inside the repo. It is not an X, Reddit, Discord, newsletter, launch, or announcement draft. It does not ask anyone to use the tool externally and does not create public posting materials.

## Scope Boundary

This dogfood note is an As-of diagnosis, not a prediction engine. It is not investment advice, not a claim that Entry Window Radar will succeed, and not a judgment of the operator or founder.
